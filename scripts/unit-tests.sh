#!/bin/sh
echo $SHELL
ansible --version
kubectl version --client
python3 --version
terraform version
terraform-docs version
terragrunt -version
tflint --version
tfsec --version
packer version
gcloud version
